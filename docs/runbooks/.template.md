# Runbook: <Operational Procedure Title>

- **Owner**: <team / role>
- **Severity**: P1 (immediate) | P2 (within 1h) | P3 (within 24h) | informational
- **Last validated**: YYYY-MM-DD by <name>

## When to Run This Runbook

증상 / 알람 / 트리거. 보통 CloudWatch alarm 이름을 명시 (예: `callcenter-prd-bedrock-throttle`).

## Prerequisites

- 필요한 AWS 권한 (`AdministratorAccess` / 특정 role)
- 필요한 도구 (`aws cli`, `terraform`, `psql`, `jq` 등)
- 알아야 하는 컨텍스트 / 다른 런북 (`bedrock-throttling.md`, `prompt-rollback.md`)

## Diagnosis

문제 식별 단계. 가능하면 단일 명령어 하나로 진단되도록.

```bash
# 예: SFN execution 실패율 확인
aws stepfunctions list-executions \
  --state-machine-arn $(terraform -chdir=infra/envs/prd output -raw sfn_arn) \
  --status-filter FAILED \
  --max-items 10
```

가능한 원인 표:

| 증상 | 가능 원인 | 다음 단계 |
|------|-----------|----------|
| <X> | <원인> | <Step> |

## Resolution

### Step 1: <action>

```bash
<exact command(s)>
```

기대 결과: <text>

### Step 2: <action>
...

## Verification

수정 후 정상화 확인 방법. 명령어 또는 대시보드 위젯.

```bash
# 예: 10분간 DLQ 깊이가 0으로 떨어지는지
watch -n 30 'aws sqs get-queue-attributes \
  --queue-url $(terraform output -raw classify_dlq_url) \
  --attribute-names ApproximateNumberOfMessages'
```

## Rollback

만약 본 런북의 조치가 상황을 악화시킨 경우 되돌리는 절차.

## Escalation

- 30분 내 해소 안 되면 → <on-call rotation / team channel>
- prd 영향 발생 시 → <incident-channel> 알림 + RCA 문서 시작

## Post-Incident

- RCA 문서 작성 (`docs/incidents/YYYY-MM-DD-<topic>.md`)
- 관련 ADR 갱신 / 신규 ADR 작성 검토
- 본 런북 갱신 (lessons learned)
