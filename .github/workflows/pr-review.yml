name: AI Code Review

# PR마다 Claude (Bedrock 호스팅) 가 변경 사항을 자동 리뷰하여 PR 코멘트로 게시합니다.
# aws-fsi-demo 의 검증된 패턴을 그대로 적용:
#   - self-hosted 러너 IAM Instance Profile 로 Bedrock 호출 (OIDC step 불필요)
#   - claude CLI: `cat diff | claude -p "$(cat prompt)" --output-format text`
#   - VERDICT: PASS|FAIL 마지막 줄 룰로 job exit code 결정
#   - <!-- callcenter-pr-review --> marker 로 코멘트 upsert (PR 업데이트마다 새 코멘트 안 쌓임)

on:
  pull_request_target:
    types: [opened, synchronize]

jobs:
  review:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: call-center-admin-claude-arm
    concurrency:
      group: pr-review-${{ github.event.pull_request.number }}
      cancel-in-progress: true
    permissions:
      pull-requests: write
      contents: read
    env:
      # Cross-region inference profile (Asia Pacific). 서울 리전 가용성 변동을 흡수.
      # 셀프호스티드 러너의 IAM Instance Profile 에 bedrock:InvokeModel 이
      # arn:aws:bedrock:ap-northeast-2::foundation-model/anthropic.claude-opus-4-*
      # + arn:aws:bedrock:ap-northeast-2:ACCOUNT:inference-profile/global.anthropic.claude-opus-4-*
      # 권한이 부여되어 있어야 함.
      ANTHROPIC_MODEL: global.anthropic.claude-opus-4-8
      CLAUDE_CODE_USE_BEDROCK: 1
      ANTHROPIC_BEDROCK_BASE_URL: https://bedrock-runtime.ap-northeast-2.amazonaws.com
      AWS_REGION: ap-northeast-2
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Verify Claude Code CLI present (pre-installed on runner)
        # call-center-admin-claude-arm 러너에는 @anthropic-ai/claude-code 가 pre-install 되어 있어야 한다.
        # docs/operations/github-actions-setup.md §2.5 참조.
        run: |
          if ! command -v claude >/dev/null 2>&1; then
            echo "::warning::claude CLI not found on runner, falling back to npm global install"
            npm install -g @anthropic-ai/claude-code
          fi
          claude --version

      - name: Get PR diff (with project-specific filters)
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          # Generated artifacts / lockfile / Lambda build dir hunks 제거.
          # 미제거 시 taxonomy_tree.json 재생성 (~128KB) 이나 staging build 가
          # diff 의 대부분을 점유해 실제 리뷰 surface 가 가려진다.
          gh pr diff "$PR_NUMBER" > /tmp/pr-diff-raw.txt
          awk '
            /^diff --git/ {
              path = substr($3, 3)
              skip = (path ~ /^src\/prompts\/v[0-9.]+\/taxonomy_tree\.(json|md)$/) ||
                     (path ~ /^infra\/modules\/[^\/]+\/build\//) ||
                     (path ~ /(^|\/)package-lock\.json$/) ||
                     (path ~ /(^|\/)yarn\.lock$/) ||
                     (path ~ /(^|\/)\.coverage$/) ||
                     (path ~ /\.terraform\.lock\.hcl$/)
            }
            !skip
          ' /tmp/pr-diff-raw.txt > /tmp/pr-diff.txt
          RAW=$(wc -l < /tmp/pr-diff-raw.txt)
          TOTAL_LINES=$(wc -l < /tmp/pr-diff.txt)
          echo "total_lines=$TOTAL_LINES" >> "$GITHUB_ENV"
          echo "Diff: $TOTAL_LINES lines (raw $RAW, filtered $((RAW - TOTAL_LINES)))"

      - name: Review with Claude Code (Bedrock Opus 4.8)
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          set -euo pipefail
          MAX_LINES=3000
          TRUNCATED=""
          if [ "${total_lines:-0}" -gt "$MAX_LINES" ]; then
            TRUNCATED="WARNING: Diff was ${total_lines} lines; only the first ${MAX_LINES} were reviewed."
          fi
          head -"$MAX_LINES" /tmp/pr-diff.txt > /tmp/pr-diff-truncated.txt

          cat <<PROMPT_EOF > /tmp/prompt.txt
          You are reviewing PR #${PR_NUMBER}: ${PR_TITLE}
          ${TRUNCATED:+
          ${TRUNCATED}}

          Read CLAUDE.md for project context. Then review the diff provided via stdin.

          Provide:
          1. **Summary** — what this PR does (2-3 sentences in Korean)
          2. **Issues** — bugs, security risks, logic errors (CRITICAL/MAJOR/MINOR)
          3. **Suggestions** — improvements
          4. **Verdict** — LGTM / Changes Requested

          Project-specific rules (call-center-admin):
          - Read CLAUDE.md + docs/architecture.md for conventions
          - Financial domain (KakaoPay): PII guard, Bedrock prompt rules, IAM least privilege
          - Check: xlsx 분류 코드 (NONEY, PAYNENT) 변경되었는지 — variant = critical
          - Check: 한글 boundary regex (\b 대신 (?<!\d)/(?!\d))
          - Check: Lambda module-level adapter cache + sys.path.insert 패턴 일관성
          - Check: Terraform KMS Resource scope, S3 lifecycle filter{}, GSI name/attr underscore 일치
          - Check: Bedrock Converse "cachePoint" 형식
          - Check: PII sweep (reason / why_rejected 에 정규식 미적용 = critical)
          - Check: confidence 타입 (bool 거부, range 0..1)
          - Check: SFN ResultPath/OutputPath 데이터 흐름 보존
          - Check: 새 ADR 작성 시 Mermaid 다이어그램 필수
          - 한국어 + 영문 기술 용어 혼용 가능. 간결하게.
          - Output ONLY the review markdown, no preamble.
          - IMPORTANT: The very last line of your output MUST be exactly one of:
            VERDICT: PASS
            VERDICT: FAIL
          Use FAIL if there are any CRITICAL or MAJOR issues. Use PASS otherwise.
          PROMPT_EOF

          cat /tmp/pr-diff-truncated.txt | claude -p "$(cat /tmp/prompt.txt)" --output-format text > /tmp/review.md

          if [ ! -s /tmp/review.md ]; then
            echo "ERROR: review output is empty" >&2
            echo "리뷰 생성 실패 — Claude CLI가 빈 응답을 반환했습니다." > /tmp/review.md
            echo "VERDICT: FAIL" >> /tmp/review.md
          fi
          echo "Review generated: $(wc -c < /tmp/review.md) bytes"

      - name: Check for blocking issues
        id: gate
        run: |
          if grep -q "^VERDICT: FAIL$" /tmp/review.md; then
            echo "result=fail" >> "$GITHUB_OUTPUT"
            echo "reason=VERDICT: FAIL" >> "$GITHUB_OUTPUT"
          elif grep -q "^VERDICT: PASS$" /tmp/review.md; then
            echo "result=pass" >> "$GITHUB_OUTPUT"
            echo "reason=No blocking issues" >> "$GITHUB_OUTPUT"
          else
            # fail-closed: VERDICT 라인이 없으면 안전하게 차단
            echo "result=fail" >> "$GITHUB_OUTPUT"
            echo "reason=No VERDICT line found — fail-closed" >> "$GITHUB_OUTPUT"
          fi

      - name: Post review comment (upsert)
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          REPO: ${{ github.repository }}
          GATE_RESULT: ${{ steps.gate.outputs.result }}
        run: |
          MARKER="<!-- callcenter-pr-review -->"
          if [ "$GATE_RESULT" = "fail" ]; then
            STATUS_BADGE="**Status: BLOCKED** — CRITICAL/MAJOR issues must be resolved before merge"
          else
            STATUS_BADGE="**Status: PASSED** — No blocking issues found"
          fi
          # 마지막 VERDICT 라인은 코멘트에서 제거 (gate 신호용)
          sed '$ { /^VERDICT:/d }' /tmp/review.md > /tmp/review-clean.md
          {
            echo "$MARKER"
            echo "## 🤖 AI Code Review (Claude Opus 4.8 on Bedrock)"
            echo ""
            echo "$STATUS_BADGE"
            echo ""
            cat /tmp/review-clean.md
            echo ""
            echo "---"
            echo "_Triggered by commit \`${{ github.event.pull_request.head.sha }}\` · workflow: \`.github/workflows/pr-review.yml\`_"
          } > /tmp/comment.md
          # 기존 marker 코멘트가 있으면 PATCH로 업서트, 없으면 새로
          EXISTING=$(gh api "repos/$REPO/issues/$PR_NUMBER/comments" \
            --jq '.[] | select(.body | contains("<!-- callcenter-pr-review -->")) | .id' 2>/dev/null | head -1)
          if [ -n "$EXISTING" ]; then
            gh api "repos/$REPO/issues/comments/$EXISTING" -X PATCH -f body="$(cat /tmp/comment.md)" 2>/dev/null || \
              gh pr comment "$PR_NUMBER" --body-file /tmp/comment.md
          else
            gh pr comment "$PR_NUMBER" --body-file /tmp/comment.md
          fi

      - name: Fail if CRITICAL or MAJOR
        if: steps.gate.outputs.result == 'fail'
        run: |
          echo "AI Review failed: ${{ steps.gate.outputs.reason }}"
          exit 1
